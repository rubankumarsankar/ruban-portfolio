import { portfolioData } from "@/data/portfolio";

export const metadata = {
  title: "Secure Comms",
  description: "Contact Rubankumar Sankar for backend engineering, API development, or collaboration on scalable web systems.",
};

export default function CommsLayout({ children }) {
  return <>{children}</>;
}
